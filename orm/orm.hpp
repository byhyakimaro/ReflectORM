#pragma once
// orm/orm.hpp
// Responsibility: Convenience single-include for the entire ORM library.
// Users only need: #include "orm/orm.hpp"
//
// Include order respects dependency graph (leaf headers pulled first).

// ── Core types ───────────────────────────────────────────────────────────────
